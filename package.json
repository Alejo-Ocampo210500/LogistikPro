{
  "name": "logistikpro",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/frontend",
    "lint": "npm run lint --workspace=apps/frontend",
    "docker:up": "docker compose -f docker/compose/docker-compose.yml up --build -d",
    "docker:down": "docker compose -f docker/compose/docker-compose.yml down",
    "docker:logs": "docker compose -f docker/compose/docker-compose.yml logs -f"
  }
}
